generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  extensions = [vector]
}

// Models added by /feature per docs/PLAN.md Section 3 (Repository, CodeChunk, ChatMessage)
